// Thin wrapper around vendored branch/position reference data.
// Source of truth lives in `_vendor/loovis-sandbox` (git subtree) so we can pull upstream updates.
export * from '../../_vendor/loovis-sandbox/src/data/branchData';

